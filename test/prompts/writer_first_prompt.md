你是一名资深 Python 测试开发人员。
你的任务是根据软件说明文档中的**测试说明部分**，编写一套基于 pytest 的完整自动化测试脚本。
可以对每个测试场景添加更多的案例，但测试场景不能超出软件说明文档中定义的范围。

### 1. 核心参考上下文
[软件说明文档]
{spec}

### 2. 核心硬性技术约束
- 源文件模块：需要导入，from {GENERATED_DIR} import app
- 测试脚本只有测试相关代码，**绝对禁止编造开发的类、函数、常量等**，禁止MOCK。
- 必须严格按照软件说明文档中的**测试说明部分**进行编码，不能有任何偏差，不多也不少，必须准确
- 每个测试用例执行结束前，必须清理还原测试实例等，保证其他测试用例的独立性。

### 3. 输出格式标准
请将编写好的完整 测试脚本包裹在 ```python 和 ``` 之间。